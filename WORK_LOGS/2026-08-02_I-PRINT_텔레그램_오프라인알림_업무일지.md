# 2026-08-02 I-PRINT 텔레그램 오프라인 알림 연결 업무일지

## 작업 목적

5800X와 Mac mini가 오프라인/복구 상태가 되었을 때 텔레그램으로 알림을 보내도록 I-PRINT 대시보드에 최소 수정으로 감시 기능을 추가했다.

## 백업

- `/home/bourne/I-print/Backup/I-PRINT_WORKING_20260802_145146_telegram_alert_before/app.py.bak`
- `/home/bourne/I-print/Backup/I-PRINT_WORKING_20260802_145146_telegram_alert_before/i-print-dashboard.service.bak`

## 수정 파일

- `/home/bourne/I-print/app.py`
- `/home/bourne/I-print/.gitignore`

## 구현 내용

### 1) 텔레그램 알림 감시 스레드 추가

- FastAPI startup 시 백그라운드 감시 스레드 실행
- 5분(300초) 간격으로 5800X와 Mac mini 상태를 확인
- 오프라인 / 복구 상태 변화 시 텔레그램 전송

### 2) 알림 발송 방식

- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_ALLOWED_CHAT_IDS`

위 환경값을 사용하도록 구성

### 3) 상태 저장

- 감시 상태를 `/home/bourne/I-print/Backup/telegram_alert_state.json`에 저장하도록 구성
- 백업 폴더는 Git 추적 제외

### 4) 인증 401 회피

- 감시 스레드가 자기 HTTP API를 다시 호출하면 인증 401이 발생해서,
  `get_5800x_status()` / `get_macmini_status()`를 직접 재사용하는 방식으로 수정

## 검증 결과

- Python 문법 검사: PASS
- 서비스 재기동 후 `i-print-dashboard.service` active 확인
- `/health` 응답 정상 확인
- 서비스 로그에서 startup 완료 확인
- 401 문제를 직접 함수 호출 방식으로 해소
- Git push 완료

## Git

- 로컬 HEAD: `58fcc7e`
- origin/main: `58fcc7e`
- 원격 push 완료

## 남은 상태

- 작업 트리에는 기존부터 있던 `index.html`, `mobile.html` 수정분이 남아 있다.
- 이번 작업에서 새로 생긴 미추적 파일은 없도록 `.gitignore`에 `Backup/`을 추가했다.

## 2026-08-02 추가 작업: 프린터 대기열 3분 초과 알림

### 추가 목적

프린터가 아직 연결되지 않은 상태라도 `printer_jobs` 대기열이 3분을 넘기면 텔레그램 알림을 보내도록 최소 기능을 추가했다.

### 추가 구현

- `printer_jobs` 테이블의 `Queued` / `Queue` / `Waiting` 상태를 5분마다 확인
- `wait_time`을 분 단위로 해석해서 3분 이상이면 알림
- 초과 대기 작업 수, 최대 대기 시간, 최대 대기 작업 정보를 메시지에 포함
- 대기열이 3분 이하로 내려가면 해소 알림 전송

### 추가 검증

- `printer_jobs.db`에 3분 초과 대기 작업이 실제로 존재하는지 확인
- 메시지 생성 함수를 가로채서 실제 출력 문구를 검증
- Python 문법 검사 PASS
- 서비스 재기동 후 active 확인
